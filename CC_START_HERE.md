# Claude Code: canonical AISTATS branch

The active paper-development branch is:

```text
aistats-current
```

Before reading the manuscript, changing code, or running experiments, execute:

```bash
git fetch origin
git checkout aistats-current
git pull --ff-only origin aistats-current
```

Then follow the `CC_START_HERE.md` file on that branch.

`main` is retained only as the public-data/source baseline. Do not use historical branch names or reconstruct older protocols from archived prompts.
