"""Clean paper-only path for the V2 flow-separation adjudication.

Separate from `asymode.dynamics` on purpose: that module carries the historical
variant zoo and must not grow another arm. This package contains exactly the
competition-informed asymmetric scaffold and nothing else.
"""
